import { Dimensions, StyleSheet } from 'react-native'
import { theme } from '../../global/styles/themes'


export const styles = StyleSheet.create({
  container: {
    height: 50,
    borderRadius: 40,
    backgroundColor: '#575AC3',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 25
  },
  text: {
    textAlign: 'center',
    color: 'white'
  },
})
